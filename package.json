{
  "name": "flow-daemon",
  "version": "0.2.0",
  "private": true,
  "description": "Playwright-driven daemon + CLIs for generating images and videos via labs.google/flow",
  "main": "server.js",
  "bin": {
    "flow-cli": "./bin/flow-cli.js",
    "flow-video-cli": "./bin/flow-video-cli.js"
  },
  "scripts": {
    "start": "node server.js",
    "daemon": "node server.js",
    "test": "node --test test/daemon.test.js test/video.test.js",
    "test:e2e": "node test/e2e-wizard.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "playwright": "^1.47.0"
  },
  "engines": {
    "node": ">=18"
  }
}
